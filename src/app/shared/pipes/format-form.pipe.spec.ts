import { FormatFormPipe } from './format-form.pipe';

describe('FormatFormPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatFormPipe();
    expect(pipe).toBeTruthy();
  });
});
